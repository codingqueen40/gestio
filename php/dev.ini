; ===== DEV ONLY =====
; Mounted by docker-compose.override.yml into conf.d/ (after zz-app.ini, so it wins).
; Re-enables on-screen error display for local debugging.
; NEVER loaded in prod (prod.yml does not mount this file).
display_errors = On
